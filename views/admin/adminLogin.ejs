<%- include('../layouts/header.ejs') %>
<section class="login_box_area section-margin">
  <div class="container">
    <div class="row">
      <div class="col-lg-4">
        
      </div>
      <div class="col-lg-4">
        <div class="login_form_inner">
          <h3>Welcome Admin</h3>
          <% if(typeof message!=='undefined'){ %>
            <h6 class="text-danger"><%=message %></h6>
            <% } %>
          <form
            class="row login_form"
            action="/admin"
            name="contactForm"
            method="post"
            onsubmit="return validateform()"
          >
            <div class="col-md-12 form-group">
              <input
                type="text"
                class="form-control"
                id="email"
                name="email"
                placeholder="Email"
                required
              />
            </div>
            <div id="errorContainer1" class="text-danger"></div>
            <div class="col-md-12 form-group">
              <input
                type="password"
                class="form-control"
                id="password"
                name="password"
                placeholder="Password"
                required
              />
            </div>
            <div id="errorContainer2" class="text-danger"></div>
            <div class="col-md-12 form-group">
              <button
                type="submit"
                value="submit"
                class="btn btn-primary"
              >
                Log In
              </button>
              <!-- <a href="#">Forgot Password?</a> -->
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</section>
<script>
  let email = document.getElementById("email");
  let password = document.getElementById("password");
  email.addEventListener("input", () => {
    if (email.value.trim() == "") {
      email.setCustomValidity("email cannot be null");
    } else {
      email.setCustomValidity("");
    }
  });
  password.addEventListener("input", () => {
    if (password.value.trim() == "") {
      password.setCustomValidity("password  number cannot be null");
    } else if (password.value.length < 6) {
      password.setCustomValidity("password  must contain 6 characters");
    } else {
      password.setCustomValidity("");
    }
  });
</script>
<%- include('../layouts/footer.ejs') %>
